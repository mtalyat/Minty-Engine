#pragma once

#include <Minty.h>

namespace mintye
{
	/// <summary>
	/// Holds data and runs the game engine application.
	/// </summary>
	class Application
	{
	public:
		Application();

		void run(int argc, char const* argv[]);

	private:

	};
}